{
  "devCommand": "pip install -r requirements.txt && python -m uvicorn api.main:app --reload",
  "outputDirectory": ".",
  "routes": [
    {
        "src": "/(.*)",
        "dest": "api/index.py"
    }
  ],
  "functions": {
    "api/index.py": {
      "maxDuration": 60
    }
  }
}